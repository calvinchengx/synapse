[project]
name = "synapse-docs"
version = "0"
description = "MkDocs / Material toolchain for Synapse user documentation (not published to PyPI)"
requires-python = ">=3.10"
dependencies = []

[dependency-groups]
docs = [
    "mkdocs-material>=9.5.0,<10",
]

[tool.uv]
package = false
